# ── Server ──
PORT=4000
FRONTEND_ORIGIN=http://localhost:3000

# ── PostgreSQL (DiNc migration target — Step 0+1) ──
PGHOST=localhost
PGPORT=5432
PGDATABASE=DiNc
PGUSER=postgres
PGPASSWORD=tmdl2026

# ── JWT ──
JWT_SECRET=DiNC_Development_JWT_Secret_2026_Change_Before_Production_94x!kQ
JWT_EXPIRES_IN=8h
