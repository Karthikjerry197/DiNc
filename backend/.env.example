# ── Server ──
PORT=4000
FRONTEND_ORIGIN=http://localhost:3000

# ── PostgreSQL ──
PGHOST=localhost
PGPORT=5432
PGDATABASE=dinc
PGUSER=dinc_app
PGPASSWORD=change-me

# ── JWT ──
JWT_SECRET=generate-a-long-random-secret
JWT_EXPIRES_IN=8h
