import RoleWorkspace from '@/components/admin/RoleWorkspace';

/** Role Workspace — opens in create mode (Milestone 3B). */
export default function NewRolePage() {
  return <RoleWorkspace initialMode="new" />;
}
