import RoleWorkspace from '@/components/admin/RoleWorkspace';

/**
 * Role Workspace (Milestone 3B) — master–detail entry point. Opens with the
 * first role selected; the left panel lists every role for selection.
 */
export default function RolesPage() {
  return <RoleWorkspace />;
}
