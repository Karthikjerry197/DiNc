import UserWorkspace from '@/components/admin/UserWorkspace';

/** Add User — opens the User Workspace in create mode (Milestone 2). */
export default function NewUserPage() {
  return <UserWorkspace mode="new" />;
}
