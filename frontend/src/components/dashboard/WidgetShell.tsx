'use client';

// WidgetShell is superseded by WidgetFrame in Dashboard Studio.
// Kept as a no-op stub so that any stale import does not cause a build error.
export default function WidgetShell() { return null; }
